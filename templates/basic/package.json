{
  "name": "agentsync-playwright-project",
  "version": "1.0.0",
  "description": "AgentSync Playwright Test Project",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "dependencies": {
    "@agentsync/playwright-framework": "^1.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.2"
  }
}